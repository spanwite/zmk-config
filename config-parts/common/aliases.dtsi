#define ALPHA         0
#define CAPS_WORD     1
#define NUM_WORD      2
#define NUMBERS       3
#define MOUSE         4
#define NAV           5
#define SYMBOLS       6
#define SHORTCUTS     7
#define POWERTOYS     8
#define FN            9
#define GAME_FPS      10
#define MEDIA         11
#define SYSTEM        12

#define ___ &trans
#define XXX &none

#define MAKE_MEH(KEY)      LC(LS(LA(KEY)))
#define MAKE_SEMI_MEH(KEY) LS(LA(KEY))
#define MAKE_HYPER(KEY)    LC(LS(LA(LG(KEY))))

#define CHANGE_LANG       &kp LC(LSHFT)

#define TOGGLE_RECORD     &kp MAKE_MEH(F1)
#define TOGGLE_OVERLAY    &kp MAKE_MEH(F2)

#define M_SPEED_UP        &kp MAKE_MEH(F3)
#define M_SLOW_DOWN       &kp MAKE_MEH(F4)

#define PT_RUN            &kp MAKE_HYPER(F1)
#define PT_COL_PICK       &kp MAKE_HYPER(F2)
#define PT_PIN            &kp MAKE_HYPER(F3)
#define PT_RULER          &kp MAKE_HYPER(F4)
#define PT_FANCY_ZONES    &kp MAKE_HYPER(F5)
#define PT_EXTRACTOR      &kp MAKE_HYPER(F6)
#define PT_MIC            &kp MAKE_HYPER(F7)